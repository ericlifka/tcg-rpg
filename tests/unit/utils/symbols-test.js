import symbols from 'tcg-rpg/utils/symbols';
import { module, test } from 'qunit';

module('Unit | Utility | symbols', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = symbols();
    assert.ok(result);
  });
});
