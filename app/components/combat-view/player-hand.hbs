<div class="player-hand">

  {{#each @cards as |card|}}
    <div class="card {{card.color}}">
      <div class="header">{{card.name}}</div>
      <button class="effect">{{card.effect}}</button>
      <button class="footer">{{color-symbol card.color}}</button>
    </div>
  {{/each}}

</div>